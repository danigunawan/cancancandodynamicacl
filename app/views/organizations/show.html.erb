<% if defined?(@organization) %>
<h1><%= @organization.name %></h1>
<% if @users.length > 0 %>
  <ul>
    <% @users.each do |user| %>
      <li><%= link_to user.email, user_path(user) %></li>
    <% end %>
  </ul>
<% end %>
<% end %>
